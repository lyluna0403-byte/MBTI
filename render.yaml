services:
  - type: web
    name: mbti-api
    env: python
    plan: starter
    buildCommand: "python3 -m py_compile server.py"
    startCommand: "python3 server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: PORT
        value: 10000
